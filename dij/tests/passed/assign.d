/*this test :
    performs assignements
    performs an addition
    nests function declarations
    uses a return value
    executes a function written in dij
    currys a function
*/
!!(entry-point)
{

x := 
  { a := b + c }.5;
!!(c-symbol)printf.[c-string:result : %d\n].(x.2.).
}
